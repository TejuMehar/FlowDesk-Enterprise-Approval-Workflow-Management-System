# ===================================================================
# FlowDesk Frontend - Environment Variables
# -------------------------------------------------------------------
# Copy this file and rename the copy to ".env"
#
# IMPORTANT: in Vite every variable MUST start with "VITE_",
# otherwise React cannot read it.
# ===================================================================

# The address where the backend is running
VITE_SERVER_URL=http://localhost:8000
